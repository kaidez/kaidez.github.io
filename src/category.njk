---
layout: layouts/base.njk
title: "Personal"
description: "Personal thoughts, experiences, and reflections"
permalink: "/personal-kai/"
---

<div class="container">
    <header class="category-header">
        <h1>Personal</h1>
        <p class="category-description">
            Personal thoughts, experiences, and reflections on life and career.
        </p>
    </header>
    
    <div class="posts-grid">
        {% for post in collections.personal %}
        <article class="post-card">
            <h3>
                <a href="{{ post.url }}">{{ post.data.title }}</a>
            </h3>
            
            <div class="post-meta">
                <time datetime="{{ post.date | htmlDateString }}">
                    {{ post.date | readableDate }}
                </time>
                
                {% if post.data.tags %}
                <div class="post-tags">
                    {% for tag in post.data.tags %}
                    {% if tag != "posts" %}
                    <span class="tag">#{{ tag }}</span>
                    {% endif %}
                    {% endfor %}
                </div>
                {% endif %}
            </div>
            
            {% if post.data.excerpt %}
            <p class="post-excerpt">{{ post.data.excerpt }}</p>
            {% else %}
            <p class="post-excerpt">{{ post.templateContent | excerpt }}</p>
            {% endif %}
        </article>
        {% endfor %}
        
        {% if collections.personal.length == 0 %}
        <div class="no-posts">
            <p>No personal posts yet. Check back soon!</p>
            <a href="/" class="button">Back to Home</a>
        </div>
        {% endif %}
    </div>
</div>

<!-- SIMPLE VERSION: src/tutorials.njk -->
---
layout: layouts/base.njk
title: "Tutorials"
description: "Step-by-step tutorials and how-to guides"
permalink: "/tutorials/"
---

<div class="container">
    <header class="category-header">
        <h1>Tutorials</h1>
        <p class="category-description">
            Step-by-step tutorials and comprehensive guides to help you learn new skills.
        </p>
    </header>
    
    <div class="posts-grid">
        {% for post in collections.tutorials %}
        <article class="post-card">
            <h3>
                <a href="{{ post.url }}">{{ post.data.title }}</a>
            </h3>
            
            <div class="post-meta">
                <time datetime="{{ post.date | htmlDateString }}">
                    {{ post.date | readableDate }}
                </time>
                
                {% if post.data.tags %}
                <div class="post-tags">
                    {% for tag in post.data.tags %}
                    {% if tag != "posts" %}
                    <span class="tag">#{{ tag }}</span>
                    {% endif %}
                    {% endfor %}
                </div>
                {% endif %}
            </div>
            
            {% if post.data.excerpt %}
            <p class="post-excerpt">{{ post.data.excerpt }}</p>
            {% else %}
            <p class="post-excerpt">{{ post.templateContent | excerpt }}</p>
            {% endif %}
        </article>
        {% endfor %}
        
        {% if collections.tutorials.length == 0 %}
        <div class="no-posts">
            <p>No tutorial posts yet. Check back soon!</p>
            <a href="/" class="button">Back to Home</a>
        </div>
        {% endif %}
    </div>
</div>

<!-- Use similar pattern for coding-best-practices.njk and reviews.njk -->