---
layout: layouts/base.njk
title: "Categories"
description: "Browse posts by category"
permalink: "/categories/"
---
<div class="container">
    <header class="page-header">
        <h1>Browse by Category</h1>
        <p>Explore posts organized by topic and theme.</p>
    </header>
    
    <div class="categories-grid">
        {% for category in collections.categories %}
        {% set categoryPosts = collections.posts | selectattr("data.tags", "defined") | selectattr("data.tags", "includes", category) %}
        
        <div class="category-card">
            <h3>
                <a href="/categories/{{ category | slugify }}/">{{ category | title }}</a>
            </h3>
            
            <p class="category-count">
                {{ categoryPosts.length }} post{% if categoryPosts.length != 1 %}s{% endif %}
            </p>
            
            {% if categoryPosts.length > 0 %}
            <div class="category-recent">
                <h4>Recent Posts:</h4>
                <ul>
                    {% for post in categoryPosts | head(3) %}
                    <li>
                        <a href="{{ post.url }}">{{ post.data.title }}</a>
                        <time datetime="{{ post.date | htmlDateString }}">
                            {{ post.date | readableDate }}
                        </time>
                    </li>
                    {% endfor %}
                </ul>
            </div>
            {% endif %}
            
            <a href="/categories/{{ category | slugify }}/" class="view-all-link">
                View All {{ category | title }} Posts →
            </a>
        </div>
        {% endfor %}
    </div>
</div>