---
layout: layouts/base.njk
title: Tutorials & Articles about Web Development - kaidez
pagination:
  data: collections.posts
  size: 10
  alias: posts
---
<section class="container">
  <h1 class="content--hidden-text">Read the last 10 blog posts on kaidez.com</h1>
  <div class="posts-homepage-post-list">
    {% for post in posts %}
    <article class="post-card">
      <h3>
        <a href="{{ post.url }}">{{ post.data.title }}</a>
      </h3>
      <div class="post-meta">
        <time datetime="{{ post.date | htmlDateString }}">
          {{ post.date | readableDate }}
        </time>
      </div>
      <p class="post-excerpt">{{ post.data.excerpt }}</p>
    </article>
    {% endfor %}
  </div>

  <div class="previous-next-button-container">
    <div class="previous-next-button">
      {% if pagination.href.previous %}<a href="{{ pagination.href.previous }}" class="pagination-buttons" aria-label="Go to previous page"><i class="fa-solid fa-arrow-left-long"></i> PREVIOUS PAGE</a>{%endif%}
    </div>
    <div class="previous-next-button">
      {% if pagination.href.next %}<a href="{{ pagination.href.next }}" class="pagination-buttons" aria-label="Go to 
  next page">NEXT PAGE <i class="fa-solid fa-arrow-right-long"></i></a>{% endif %}
    </div>
  </div>
</section>
