---
layout: layouts/category.njk
title: "Coding Tips"
description: "Tips, techniques, and best practices for writing better code from kaidez"
permalink: "/coding-best-practices/"
image: default-image.jpg
---

<div class="container">

  <div>
    {% for post in collections.posts %}
    {% if post.data.tags %}
    {% set hasCodingTag = false %}
    {% for tag in post.data.tags %}
    {% if tag == "coding-best-practices" %}
    {% set hasCodingTag = true %}
    {% endif %}
    {% endfor %}

    {% if hasCodingTag %}
    <article class="post-card">
      <h2>
        <a href="{{ post.url }}">{{ post.data.title }}</a>
      </h2>

      <div class="post-meta">
        <time datetime="{{ post.date | htmlDateString }}">
          {{ post.date | readableDate }}
        </time>
      </div>

      {% if post.data.excerpt %}
      <p class="post-excerpt">{{ post.data.excerpt }}</p>
      {% endif %}
    </article>
    {% endif %}
    {% endif %}
    {% endfor %}
  </div>
</div>